- words   = %w{Ready Open Closed Final}
%table.table.table-bordered
  %thead
    %th Code
    %th Year
    %th Sport
    %th Status
    %th Games
  %tbody
  - @teams.each do |team|
    %tr
      %td
        %a{href: "/admin/#{team.code}/"}= team.code
      %td= team.year
      %td= team.sport
      %td
        %a.btn.btn-primary{href: "/admin/#{team.code}/"}= words[team.status_code]
      %td= team.games.length
%br
